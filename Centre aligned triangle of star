//used youtube to understand the code logic 
let n = prompt("Enter the number of rows");
for (let i = 0; i < n; i++) {
  let spaces = ' '.repeat(n - i - 1);       
  let stars = '*'.repeat(2 * i + 1);        
  console.log(spaces + stars);              
}

